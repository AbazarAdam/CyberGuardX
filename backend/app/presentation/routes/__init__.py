"""
Presentation — Route Package
==============================
One module per API feature:

- ``email.py``       → ``POST /check-email``
- ``url.py``         → ``POST /check-url``
- ``password.py``    → ``POST /check-password``, ``POST /generate-password``
- ``scanner.py``     → ``POST /scan-website``, scan details, progress, reports
- ``history.py``     → ``GET  /scan-history``
"""
